// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext {
        buildToolsVersion = findProperty('android.buildToolsVersion') ?: '36.0.0'
        // Yandex MapKit 4.42 requires API 26+; keep the requirement explicit
        // rather than overriding the manifest merger with an unsafe override.
        minSdkVersion = Integer.parseInt(findProperty('android.minSdkVersion') ?: '26')
        // Google Play: с 31.08.2026 требуется targetSdk >= 36 (Android 16).
        compileSdkVersion = Integer.parseInt(findProperty('android.compileSdkVersion') ?: '36')
        targetSdkVersion = Integer.parseInt(findProperty('android.targetSdkVersion') ?: '36')
        kotlinVersion = findProperty('android.kotlinVersion') ?: '1.9.25'

        ndkVersion = "26.1.10909125"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath('com.android.tools.build:gradle')
        classpath('com.facebook.react:react-native-gradle-plugin')
        classpath('org.jetbrains.kotlin:kotlin-gradle-plugin')
    }
}

apply plugin: "com.facebook.react.rootproject"

allprojects {
    // Yandex MapKit 4.42 brings WorkManager/Kotlin 2.1 transitively. Expo
    // SDK 52 is built with Kotlin 1.9, so keep the compatible versions for
    // the whole graph instead of upgrading the Expo toolchain.
    configurations.configureEach {
        resolutionStrategy.force(
            'androidx.work:work-runtime:2.7.1',
            'org.jetbrains.kotlin:kotlin-stdlib:1.9.25',
            'org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.9.25',
            'org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.9.25'
        )
    }
    repositories {
        maven {
            // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
            url(new File(['node', '--print', "require.resolve('react-native/package.json')"].execute(null, rootDir).text.trim(), '../android'))
        }
        maven {
            // Android JSC is installed from npm
            url(new File(['node', '--print', "require.resolve('jsc-android/package.json', { paths: [require.resolve('react-native/package.json')] })"].execute(null, rootDir).text.trim(), '../dist'))
        }

        google()
        mavenCentral()
        maven { url 'https://www.jitpack.io' }
    }
}
