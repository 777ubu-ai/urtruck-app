# Метрики карты: честные неизвестные значения

База a4b94f7f, QA-ветка. F08: без свежего GPS остаток, пройдено и прогресс показывают «—», а не полный маршрут/0%. Срок доставки больше не выдаётся за ETA. Время маршрута подписано как оценка на RU/EN/KK/ZH. Загрузка точки не выдаётся за «обновлено сейчас». Возраст точки пересчитывается каждые 15 секунд даже без успешного polling. Native/web не превращают null/пустые координаты в 0,0; валидные нулевые координаты сохраняются. Нулевой остаток разрешён. Геометрия native обновляется по самой geometry, включая externalRoute без routeKey. Web MultiRoute по plannedPoints маркируется полным маршрутом.

663/663 frontend, lint 377 JS, i18n и nav PASS. Добавлены четыре проверки корректности значений/координат; прежний static test, требовавший progress||0, обновлён при сохранении проверки реальных route properties. Native renderer прежний react-native-yamap; build env EXPO_PUBLIC_YANDEX_MAPKIT_API_KEY. Геометрию маршрутного провайдера не заменяли.

Открытые дефекты: ETA с учётом актуального движения/пограничных задержек не реализован; фактическое время в движении не вычисляется (показывается оценка маршрута); projection по geometry требует проверки ухода с маршрута/петель. Нет физического MapKit acceptance в этом пакете. Эти пункты не PASS и не 10/10.

Evidence: /private/tmp/urtruck-security-20260916-i3Bkql/{frontend-metrics-v2.log,lint-metrics.log,i18n-final.log,nav-final.log}.
