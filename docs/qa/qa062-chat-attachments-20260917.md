# QA062 — чат, вложения и задержки, 17.09.2026

Статус: проверенный delta QA062 сохранён. RELEASE READY: NO. Финальный physical acceptance не закрыт.

## Область проверки
Продолжение канонической ветки `qa/master-hard-qa-20260916`.
Приоритет владельца: быстрая доставка сообщений, RU↔ZH, фотографии/документы, native push со звуком.
Сессии, история, тестовые сделки и исходные доказательства сохранены.

## Изменения
- `584557fd`: ограниченный кеш приватных signed URL с обновлением до истечения, защита от параллельных повторов; блокирующие операции storage вынесены из event loop.
- Тот же коммит: фото ограничивается длинной стороной 1600 px, без увеличения небольших исходников; JPEG quality 0.85, целевой размер 1000 KB с ограниченными дополнительными проходами (это целевой, не гарантированный предел).
- Для фото добавлены индикатор отправки и retry. После полученного upload receipt повтор использует тот же объект и client message ID; двойное нажатие защищено.
- PDF/XLS/XLSX/CSV передаются без преобразования в изображение.
- `765fbe8d`: скачиваемый приватный документ сохраняет исходное имя.
- `bc8c5a7b`: push о фото и документах локализуется по языку получателя RU/KK/ZH/EN; пользовательские подписи и имена файлов не переводятся.
- Backend изменения выложены с резервными копиями и проверкой исходных SHA. Изменений схемы БД нет.

## Проверки кода
- Frontend: 699/699.
- Lint: PASS.
- Backend, полный финальный прогон: 878/878.
- Первый полный backend-прогон выявил два сбоя тестового Linux storage path на Mac; после задания отдельного временного каталога проверки прошли. Это не было ошибкой live API.
- Graphify AST-only выполнен до/после; артефакты сохранены отдельно от git.

## Измерения
| Сценарий | Результат | Ограничение |
|---|---|---|
| История той же комнаты, до исправления | 3.382 s | Loopback API, 36 сообщений, 9 приватных media |
| Повторная история после исправления | 0.052 / 0.057 / 0.057 s | Прогретый кеш; не полная пользовательская задержка |
| Текст, физическое нажатие Send → видимый текст у получателя | ≤6.821 s | Включает время получения UI dump; оба телефона QA061 с обновлённым backend |
| Текст на обоих QA062, физический Send → видимый текст у получателя | ≤6.795 s | Два UI dump; верхняя граница с накладными расходами измерения, не чистая сеть |
| Новый пользовательский voice 27 s, первый RU→ZH | ≤11.551 s | Физический тап и новый китайский результат; включает UI dump |
| Повтор voice 27 s | 0.163 / 0.051 s | Loopback API, cached=true и translation_cached=true; provider=openai |

Первый замер видимости текста отброшен: получатель находился выше последних сообщений, новая запись была ниже экрана. Это не доказательство 30-секундной доставки.
Первое распознавание и перевод не следует называть мгновенными; полная 55–60 s матрица пока OPEN.

## Физические вложения на QA061 с обновлённым backend
- Shipper → Driver: PDF выбран в системном picker, появился в чате получателя.
- PDF скачан и открыт Android-просмотрщиком: данные читаемы, 1 страница.
- 1739 bytes; SHA-256 загруженного и скачанного на телефон PDF совпал с исходником.
- Driver → Shipper: CSV 41 bytes выбран в picker, появился в чате получателя.
- Background push для CSV: HIGH, messages_v2, sound URI, vibration и системное audible timestamp подтверждены. Тап открыл нужную сделку.
- Offline PDF: старые сообщения сохранены, видны отдельная ошибка обновления истории и retry документа.
- После восстановления сети retry завершился; один uploaded объект на client upload, дубликата в БД нет.
- Для китайского получателя push нового PDF: «📄 新文件», исходное имя, HIGH и audible timestamp.
- Исправленное имя скачивания подтверждено HTTP 200 и Content-Disposition с исходным именем; содержимое не изменилось.
- Подтверждение звука на слух владельцем отдельно не получено.

## Android candidate
[GitHub Actions 35231193753](https://github.com/777ubu-ai/urtruck-app/actions/runs/35231193753): SUCCESS.
Source: `584557fd94ce7ba5db5fa780f9c37da95ca1719a`.
Проверенный package: `com.urtruck.app.qa2`, versionCode `211040062`, versionName `1.0.8`.
APK SHA-256: `d19ed4ef24200a7904cca819c05195d46e2f6a264c2957589e31e8f6e5f98175`.
Signing cert SHA-256: `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`.
Оба Xiaomi обновлены через install -r, versionCode проверен. Сессии, роли RU Shipper / ZH Driver, история и вложения сохранились.
Сборка QA с debug signing; не store candidate.

## Физические фото и push на QA062
- Реальный системный picker: Shipper → Driver и Driver → Shipper. Фото доставлены и открыты на обоих получателях.
- Тестовое изображение: 3000×4000, 416837 bytes → JPEG 1200×1600, 70211 bytes (примерно на 83% меньше). Соотношение сторон сохранено.
- На обоих экранах читаются маршрут, 20 000 kg, 80 boxes, USD 7 800, дата/время и номер пломбы. Это один синтетический образец; не доказательство качества любого мелкого скана.
- Offline фото: история сохранена, локальный снимок и «Нет связи с сервером · Повторить» видимы. После сети и retry фото доставлено; три намеренные отправки — три записи, повторяющихся client message ID нет.
- Два обычных отправления и retry дали одинаковый SHA-256 сжатого файла: `131422435df96c8a2ce46ea75490aea1d5ba5f40da5e7b68aa77f6b12b437910`.
- Native background photo push на QA062: RU «📷 Фото / Новое фото в чате», ZH «📷 照片 / 收到新照片»; HIGH, звук, вибрация, положительный Android audible timestamp.
- Тап по RU и ZH photo push открыл ту же сделку; Back виден, новая фотография присутствует после загрузки.
- Driver → Shipper PDF повторён на QA062: доставлен, 1739 bytes; push «📄 Новый документ» с именем файла и положительным audible timestamp.
- Группировка двух разных уведомлений Android не считается дублем события. Слуховое подтверждение владельца остаётся OPEN.
- PDF открыт через внешний Android-просмотрщик; встроенный просмотр PDF и физическое открытие CSV здесь не заявляются.

## Оставшиеся gates
- Расширенная выборка фото/сканов и крупных файлов; XLS/XLSX физически в этом delta не проверялись. Потеря ответа после storage upload отдельно не симулировалась.
- Длинный реальный voice 55–60 s в обе стороны; ZH→RU voice, persistence после relaunch, полная cache/provider evidence.
- Полная push matrix QA062, звук на слух, OPPO.
- GPS короткий background/screen-off, Location degraded/recovery и terminal stop; прежний FIFO PASS не закрывает эти пункты.
- Map road route latency/reliability.
- Android release signing, iOS/TestFlight и store declarations — после physical acceptance.

Raw UI/XML/PNG, логи, фикстуры, замеры и Graphify сохранены на Mac отдельно. Автоповорот обоих Xiaomi восстановлен в исходное состояние; Wi-Fi и мобильная сеть включены. Проверка завершающих настроек сохранена.
