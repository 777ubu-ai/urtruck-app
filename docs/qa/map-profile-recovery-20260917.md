# UrTruck: восстановление маршрута и профиля — 17.09.2026

Ветка: `qa/master-hard-qa-20260916`. База пакета: `1408644a`.
Вердикт релиза: **BLOCKED**, оценка 10/10 не заявляется.

## Фактически выполнено

- Backend `1408644a` установлен с резервной копией кода, PM2-конфигурации и согласованной SQLite-копией. Существующие функции сервера сохранены. До переключения: полный backend 854/854 PASS; после переключения health 200, API и scheduler online. Очистки данных не было.
- QA2 `211040058` установлен через `adb install -r` на двух Xiaomi. Сертификат совпал с предыдущей сборкой. SHA256 APK: `ad0715a4227ae355dece535d9551c36e49faa65001f39e1cec2f0a76b7b76c2f`.
- Вход в существующие QA-аккаунты Boris/shipper и Serik/driver выполнен через интерфейс. На обеих ролях видны четыре корневые вкладки.
- На Xiaomi грузоотправителя физически открылись нативные Яндекс-тайлы. Обнаружен независимый сбой дорожного маршрута, а не отсутствие MapKit key.

## Найденные и исправленные дефекты

1. ORS отклонял длинный маршрут по лимиту 6000 км. Прежний fallback делил запрос в линейной промежуточной точке, оказавшейся вне дорожной сети: HTTP 404/code 2010. Теперь синтетические границы подтверждает HGV snap endpoint; максимум пять кандидатов на границу, радиус 2 км, исходные адреса сохранены. Непривязанные точки и разрыв между геометриями более 5 м приводят к ошибке, а не к выдуманной линии. Максимум 16 участков.
2. Неудачный ORS-запрос на китайском направлении выполнялся дважды за один HTTP-запрос. Повтор одного и того же провайдера исключён; другой провайдер остаётся доступным.
3. Нативная карта после HTTP 502 бесконечно показывала «Строим маршрут». Добавлены ошибка, ручной повтор, отмена при выходе и ограничение ожидания. Повреждённая геометрия отвергается целиком.
4. Back профиля находился внутри ScrollView и исчезал при прокрутке. Шапка закреплена вне списка; область кнопки 44×44.
5. На Android 058 профиль показывал номер iOS `(49)`. Версия берётся из установленного APK/IPA через expo-application, fallback строго по платформе.

## Проверки пакета

- Lint: 383 активных JS-файла PASS.
- Frontend/unit: 685/685 PASS.
- Navigation contract PASS; i18n: RU/EN/KK/ZH по 2003 ключа, пропусков нет.
- Backend routing: 18/18 PASS.
- Реальный вызов candidate-кода с действующим ORS credential, Иу→Москва, payload 18 т: `ok=true`, `driving-hgv`, 9 062 755 м, 418 694 с движения, 4 046 точек, два участка. Прогноз с отдыхом 1 066 694 с — оценка, не фактическое время доставки/очередей на границе.
- Graphify построен от 1408644a, зависимости TruckMap/Profile и общих utility проверены. Артефакт графа сохранён вне git.

## Открыто

- Физический retest маршрута, Back/версии профиля на новом APK 059.
- ETA в интерфейсе пока отсутствует; не подменять вычислением из прямой или старого GPS.
- OpenAI production translation вернул HTTP 429 `credit_balance_exhausted`. GitHub billing уже не мешает сборке, но не оплачивает голосовой провайдер. RU↔ZH voice intelligence не PASS. Проверенная локальная альтернатива пока не прошла качество топонимов.
- Полный короткий чат/push smoke, 55–60 секунд голоса, persistence/cache, GPS offline/recovery и полный acceptance не закрыты этой записью.
- OPPO и повторные 15/30-минутные GPS-прогоны исключены владельцем; не выдавать исключение за PASS.

Документация ORS: https://giscience.github.io/openrouteservice/api-reference/endpoints/snapping/ ; https://openrouteservice.org/restrictions/ .
Сборка 058: https://github.com/777ubu-ai/urtruck-app/actions/runs/35179129924 .
Физические доказательства на Mac: `/private/tmp/urtruck-physical-058-20260917/`.
