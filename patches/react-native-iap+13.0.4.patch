diff --git a/node_modules/react-native-iap/android/build.gradle b/node_modules/react-native-iap/android/build.gradle
index e4a79cc..f9a4bbc 100644
--- a/node_modules/react-native-iap/android/build.gradle
+++ b/node_modules/react-native-iap/android/build.gradle
@@ -164,7 +164,11 @@ dependencies {
   testImplementation "junit:junit:4.13.2"
   testImplementation "io.mockk:mockk:1.13.5"
 
-  playImplementation "com.android.billingclient:billing-ktx:$playBillingSdkVersion"
+  // PBL >= 8 собрана с Kotlin 2.x, а проект на Kotlin 1.9 — компиляция против
+  // billing-ktx падает с несовместимостью kotlin-metadata. RN-iap использует
+  // только Java-API billing (com.android.billingclient.api.*), ktx-расширения
+  // (coroutines) не нужны, поэтому тянем базовый артефакт billing.
+  playImplementation "com.android.billingclient:billing:$playBillingSdkVersion"
   playImplementation "com.google.android.gms:play-services-base:$playServicesVersion"
 
   amazonImplementation  "com.amazon.device:amazon-appstore-sdk:$amazonSdkVersion"
diff --git a/node_modules/react-native-iap/android/src/play/java/com/dooboolab/rniap/RNIapModule.kt b/node_modules/react-native-iap/android/src/play/java/com/dooboolab/rniap/RNIapModule.kt
index 70149d2..1f0d59e 100644
--- a/node_modules/react-native-iap/android/src/play/java/com/dooboolab/rniap/RNIapModule.kt
+++ b/node_modules/react-native-iap/android/src/play/java/com/dooboolab/rniap/RNIapModule.kt
@@ -13,12 +13,11 @@ import com.android.billingclient.api.ConsumeParams
 import com.android.billingclient.api.ConsumeResponseListener
 import com.android.billingclient.api.GetBillingConfigParams
 import com.android.billingclient.api.GetBillingConfigParams.Builder
+import com.android.billingclient.api.PendingPurchasesParams
 import com.android.billingclient.api.ProductDetails
 import com.android.billingclient.api.Purchase
-import com.android.billingclient.api.PurchaseHistoryRecord
 import com.android.billingclient.api.PurchasesUpdatedListener
 import com.android.billingclient.api.QueryProductDetailsParams
-import com.android.billingclient.api.QueryPurchaseHistoryParams
 import com.android.billingclient.api.QueryPurchasesParams
 import com.facebook.react.bridge.Arguments
 import com.facebook.react.bridge.LifecycleEventListener
@@ -42,7 +41,9 @@ import com.google.android.gms.common.GoogleApiAvailability
 @ReactModule(name = RNIapModule.TAG)
 class RNIapModule(
     private val reactContext: ReactApplicationContext,
-    private val builder: BillingClient.Builder = BillingClient.newBuilder(reactContext).enablePendingPurchases(),
+    private val builder: BillingClient.Builder = BillingClient.newBuilder(reactContext).enablePendingPurchases(
+        PendingPurchasesParams.newBuilder().enableOneTimeProducts().build(),
+    ),
     private val googleApiAvailability: GoogleApiAvailability = GoogleApiAvailability.getInstance(),
 ) : ReactContextBaseJavaModule(reactContext),
     PurchasesUpdatedListener {
@@ -279,7 +280,7 @@ class RNIapModule(
                 if (!isValidResult(billingResult, promise)) return@queryProductDetailsAsync
 
                 val items = Arguments.createArray()
-                for (skuDetails in skuDetailsList) {
+                for (skuDetails in skuDetailsList.productDetailsList) {
                     skus[skuDetails.productId] = skuDetails
 
                     val item = Arguments.createMap()
@@ -556,19 +557,26 @@ class RNIapModule(
         ensureConnection(
             promise,
         ) { billingClient ->
-            billingClient.queryPurchaseHistoryAsync(
-                QueryPurchaseHistoryParams
+            // Play Billing Library 8 полностью удалил queryPurchaseHistoryAsync
+            // без клиентской замены: история покупок — серверная задача
+            // (Play Developer API). Ближайший клиентский аналог —
+            // queryPurchasesAsync, который возвращает только АКТИВНЫЕ покупки
+            // аккаунта. Маппим их в ту же форму, чтобы JS-API модуля
+            // (getPurchaseHistory) продолжал работать, но для истины о
+            // просроченных/исторических покупках нужен бэкенд.
+            billingClient.queryPurchasesAsync(
+                QueryPurchasesParams
                     .newBuilder()
                     .setProductType(
                         if (type == "subs") BillingClient.ProductType.SUBS else BillingClient.ProductType.INAPP,
                     ).build(),
-            ) { billingResult: BillingResult, purchaseHistoryRecordList: MutableList<PurchaseHistoryRecord>? ->
+            ) { billingResult: BillingResult, purchasesList: List<Purchase>? ->
 
-                if (!isValidResult(billingResult, promise)) return@queryPurchaseHistoryAsync
+                if (!isValidResult(billingResult, promise)) return@queryPurchasesAsync
 
-                Log.d(TAG, purchaseHistoryRecordList.toString())
+                Log.d(TAG, purchasesList.toString())
                 val items = Arguments.createArray()
-                purchaseHistoryRecordList?.forEach { purchase ->
+                purchasesList?.forEach { purchase ->
                     val item = Arguments.createMap()
                     // Add both field names for compatibility
                     item.putString("productId", purchase.products[0])
