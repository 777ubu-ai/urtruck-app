fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pytesseract==0.3.13
Pillow==10.4.0
httpx==0.27.2
h2==4.1.0
PyJWT==2.9.0
cryptography==43.0.1
redis==5.1.0
apscheduler==3.10.4
python-multipart==0.0.12
python-dateutil==2.9.0

# CGR Integration (Stream A) — см. docs/cgr/TZ-CGR-001-v1.1.md
pydantic-settings==2.5.2
tenacity==9.0.0
beautifulsoup4==4.12.3
lxml==5.3.0
sentry-sdk[fastapi]==2.18.0

# Google Play Billing — верификация подписок (Android Publisher API).
# Lazy-imported внутри services/google_play_service.py: без креденшлов
# (GOOGLE_PLAY_SERVICE_ACCOUNT_JSON) сервис работает в MOCK-режиме и эти
# пакеты не импортируются вовсе — на проде, где REAL-режим включён, нужны.
google-api-python-client==2.149.0
google-auth==2.35.0

# MVP optional (установить через pip если используется):
# dlib==19.24.2
# face-recognition==1.3.0
# opencv-python-headless==4.10.0.84
# numpy
# boto3  # только если STORAGE_PROVIDER=s3
