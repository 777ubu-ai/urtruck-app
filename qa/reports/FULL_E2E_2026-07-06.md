# FULL E2E — обе роли, упор на EMAIL-вход (2026-07-06)

**Ветка:** `integration/build-30` (email-вход слит: `d432723`)
**Артефакт:** СОБРАННЫЙ веб-UI — `npm run build:web` → `dist/`, раздаётся через
`scripts/e2e-static-proxy.js` (`:4599`, проксирует `/api` → backend `:8001`).
**Backend:** FastAPI `:8001` в MOCK (email/SMS MOCK), dev/`BETA_MODE` → код `0000`.
Для «честного» MOCK (код в ответе) — отдельный инстанс `:8002` с `BETA_MODE=false`.
**Драйвер e2e:** Playwright (см. ниже про Maestro).
**Спека:** `tests/e2e/urtruck-full-e2e-web.spec.js` (+ `tests/e2e/helpers/webflow.js`).
**Скриншоты:** `qa/screenshots/e2e-web/*.png` (локально, gitignored) — 20 шт.

---

## Почему Playwright, а не Maestro

Maestro Web в этом окружении **не запускается**: `maestro test -p web` (и
`--platform=web`, и appId-URL) всегда рапортует `0 devices connected`. Всё нужное
на месте (Maestro 2.6.0, `maestro-web.jar`, `selenium-chromium-driver`, Chrome,
`start-device --platform web` реально поднимает `chromedriver`), но web-«устройство»
не регистрируется в раннере. Единственная JDK — **Java 26** (Temurin), web-поддержка
Maestro 2.6.0 — beta и, по всем признакам, несовместима с Java 26 / macOS 26.

Решение согласовано с владельцем: гнать ту же ПОЛНУЮ e2e на том же собранном
веб-UI через Playwright (тот же `dist/` + прокси, реальные экраны, скриншоты).
Цель и артефакт сохранены — сменился только драйвер.

**Итог прогона:** `11 passed` (~26 c).

---

## БЛОК A — EMAIL-вход (главное)

| # | Проверка | Итог | Скриншоты |
|---|----------|------|-----------|
| A1 | Клиент по email: онбординг → «Email» → `client@…` → отправка → код (BETA 0000) → verify → **в приложении** (RoleV2 → Main) | ✅ PASS | `A1_01…A1_05` |
| A2 | Водитель по email: то же → роль водитель (таб Queue есть, Publish-вкладки нет) | ✅ PASS | `A2_01` |
| A3 | Валидация: `abc@` → CTA заблокирована; неверный код → остаёмся на OTP; `consent=false` → backend **400** | ✅ PASS | `A3_01`,`A3_02` |
| A4 | Токен сохраняется: `localStorage.ur_reg_token` есть; reload → сразу приложение, без онбординга | ✅ PASS | `A4_01` |
| A5 | OtpV2: таймер повторной отправки виден; «Изменить e-mail» → назад на PhoneV2 (email-режим) | ✅ PASS | `A5_01`,`A5_02` |

Замечания:
- Повторная отправка (`resend`) залочена таймером 60 c — UI-триггер в пределах теста
  не жмётся; на уровне API повторный `email/send` работает (см. Шаг E).
- Идентификатор e-mail хранится в колонке `phone` (MVP) — подтверждено:
  `/register/me.phone == "<email>"`.

## БЛОК B — Телефонный вход не сломан

| # | Проверка | Итог | Скрин |
|---|----------|------|-------|
| B1 | Вход по номеру (`channel=phone`, +7, 10 цифр) → код → verify → **в приложении** | ✅ PASS | `B1_01` |

Email-правки телефонный флоу не задели. Примечание: при `BETA_MODE` телефонный
`wa_verify` авто-присваивает роль на бэке (driver) — отсюда прямой заход в Main.

## БЛОК C — Водитель (регрессия)

| # | Проверка | Итог | Скрин |
|---|----------|------|-------|
| C2 | Таб-бар: 5 вкладок **Feed / MyWork / Queue / Chats / Profile**, Chats отдельной вкладкой, Publish-вкладки нет; акцент driver зелёный `#00E676` | ✅ PASS | `C2_01` |
| C3 | Feed открывается (заголовок «Грузы», поиск, фильтры Направление/Дата/Кузов/Цена) | ✅ PASS | `C3_01` |
| C5 | Queue открывается (заголовок «Очереди на границах») | ✅ PASS | `C5_01` |
| C1 | Регистрация водителя (образцы-фото) | ⚪ N/A | — |
| C4 | Ставка водителя (make/edit/counter/cancel по testID) | 🟡 частично | — |

- **C1** не гонялся: driver-verification (фото-шаги) — REAL-DEVICE флоу (камера/аплоады),
  на веб-бандле не воспроизводится осмысленно.
- **C4**: постановка ставки водителем проверена на уровне API в кросс-ролевом
  сценарии D2 (`POST /market/bids` → 2xx). UI-мастер ставки (BidModal) отдельно не
  прокликан — приоритет отдан блоку A и владельческим действиям D2.
- **C5** «встать в очередь»: экран открывается, контент очереди грузится асинхронно
  (спиннер); постановка в очередь идёт через внешний CGR-портал — глубоко не драйвилось.

## БЛОК D — Грузоотправитель (регрессия) + кросс-роль

| # | Проверка | Итог | Скрин |
|---|----------|------|-------|
| D1 | Клиент публикует груз (валюта **KZT/₸**) → груз виден в «Мои грузы» (`my-cargo-card`) | ✅ PASS | `D1_01` |
| D2 | Водитель ставит ставку → владелец открывает груз: оффер виден, суммы с **₸** (₸420 000 / ₸400 000, НЕ «$»); accept → сделка создана | 🟡 PASS с багом | `D2_01`,`D2_02` |
| D4 | Клиентский таб-бар (центр — **Publish**), кнопка выхода `profile-logout` присутствует | ✅ PASS | `D4_01` |

- **D1**: груз создан через API (форма с пикерами город/кузов/дата драйвится хрупко;
  данные сидированы напрямую), UI-отражение в «Мои грузы» проверено. Валюта по
  умолчанию — ₸ KZT. `require_level(1)` — свежему email-аккаунту хватает.
- **D2 — валюта корректна**: требование «суммы с валютой груза (₸/$), НЕ `$420000`»
  выполнено — CargoDetail рендерит `formatPrice(amount, cargo.currency)` → `₸420 000`
  и ставку `₸400 000`. Голого `$420000` нет.
- **D2 — БАГ (см. ниже)**: владелец-**email** видит на своём грузе bidder-view и
  **не имеет кнопок accept/reject**. Accept выполнен через API (backend-путь рабочий:
  `POST /market/bids/{id}/accept` → сделка в `/market/deals`).
- **D3** (deal room) отдельно не прокликан — сделка создаётся (подтверждено `/deals`),
  но UI deal-room из-за того же owner-бага у email-владельца недоступен нормально.

## ШАГ E — сборка / бэкенд

| Проверка | Итог |
|----------|------|
| `npm run build:web` (прод-бандл) | ✅ exit 0, `dist/` собран |
| MOCK `email/send` (BETA off, :8002) вернул код | ✅ `mock:true, code:"3339"` |
| `email/verify` верным кодом | ✅ HTTP 200 (token) |
| `email/send` `consent=false` | ✅ HTTP 400 |
| BETA `email/verify` кодом `0000` | ✅ HTTP 200 (token) |

---

## БАГИ

### BUG-1 (P1) — email-владелец не может принять/отклонить ставку на своём грузе (UI)

**Где:** `src/utils/AuthContext.js:36` (гейт синка id) + `src/screens/CargoDetail.js:155`
(owner-check) + `backend/api/registration.py` (email_verify).
**Симптом:** зашедший по email грузовладелец на экране своего груза (CargoDetail)
видит **bidder-view**: снизу CTA «Предложить цену», у ставки нет кнопок
`bid-accept`/`bid-reject`/`bid-counter`. Принять ставку через UI невозможно.
**Корень:**
1. `email_verify` создаёт/возвращает пользователя с ролью **`guest`** и НИКОГДА её не
   меняет. Роль, выбранная на RoleV2, ставится только в клиентском состоянии
   (`AuthContext.setRole`) и на бэк не уходит (`regAPI.updateProfile` роль не шлёт;
   экран ProfileV2, который мог бы дослать, на вебе пропускается — см. Obs-1).
   Подтверждение: `GET /register/me` и `GET /users/me` для email-юзера → `role: "guest"`.
2. `refreshLevel` синкает реальный backend-id в сессию **только если**
   `me.role && me.role !== 'guest'` (AuthContext.js:36). Роль `guest` → синк
   пропускается → `session.user.id` остаётся синтетическим `u_<timestamp>`.
3. CargoDetail считает владельца как `fullCargo.owner_id === session.user.id`
   (CargoDetail.js:155). `owner_id` = реальный driver_id, `session.user.id` = синтетика
   → **не совпадают** → isMine=false → bidder-view.

**Репро:**
1. Вход по email как клиент → создать груз.
2. Любой водитель ставит ставку на этот груз.
3. Тем же email снова войти, открыть груз (Мои грузы → предложения).
4. Ожидание: кнопки Принять/Отклонить. Факт: bidder-view, кнопок нет.

**Почему у телефона нет:** `wa_verify` (BETA и реальный) присваивает роль на бэке
(driver), поэтому `refreshLevel` синкает id и owner-check срабатывает. Баг
специфичен для **email-канала**.

**Влияние:** email-владелец не может вести сделку из UI (accept/reject/counter,
deal-room). Затрагивает и любые другие `isMine`-gated элементы для email-юзеров.

**Фикс (НЕ применял — graphify-gated: backend-регистрация + AuthContext; жду решения).**
Варианты:
- (реком.) `backend/api/registration.py::email_verify` — принимать `role` (он уже есть
  в `EmailSendRequest`, добавить в verify или брать из сохранённого) и проставлять роль
  драйверу вместо `guest`; ИЛИ
- ослабить гейт в `AuthContext.js:36`, синкая `me.id` даже при `role==='guest'`
  (id не зависит от роли) — точечно и безопасно; ИЛИ
- гарантировать прохождение ProfileV2 (updateProfile) — но это меняет навигацию (Obs-1).

## НАБЛЮДЕНИЯ

### Obs-1 — ProfileV2 (имя/город) пропускается на вебе
После выбора роли на RoleV2 `setRole` делает `hasRole=true`, и `AppNavigator`
реактивно переключается на аутентифицированный стек (Main) — а `ProfileV2` живёт в
pre-auth стеке, поэтому `navigate('ProfileV2')` не успевает и юзер попадает сразу в
Main. Имя/город новых пользователей не собираются (пусты). Наблюдалось для обеих
ролей; не только email. Не баг email-канала, но усугубляет BUG-1 (роль/профиль на
бэк не дослались).

### Obs-2 — MOCK vs BETA
При `URTRUCK_ENV=development` активен `BETA_MODE` → `email/send` идёт beta-веткой
(`mock:false`, код не в ответе), verify принимает `0000`. «Честный» код в ответе —
только при `BETA_MODE=false` (проверено на :8002). Оба режима — норма для MOCK.

---

## Матрица итогов

| Блок | PASS | Частично | N/A | FAIL/Bug |
|------|------|----------|-----|----------|
| A (email) | A1,A2,A3,A4,A5 | — | — | — |
| B (телефон) | B1 | — | — | — |
| C (водитель) | C2,C3,C5 | C4 | C1 | — |
| D (клиент) | D1,D4 | D2*,D3 | — | BUG-1 (D2 UI-accept) |

\* D2: валюта/оффер-UI ✅, UI-accept заблокирован BUG-1 (accept подтверждён через API).

**Вывод:** новый EMAIL-вход работает для входа обеих ролей (A1–A5 ✅), телефонный
вход не сломан (B1 ✅), базовая навигация/структура обеих ролей ✅, валюта ₸ ✅.
**Один P1-баг** (BUG-1): email-владелец не управляет ставками из UI — требует решения
по способу фикса (backend-роль / гейт id-синка).
