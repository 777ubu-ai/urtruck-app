# MASTER BRIEF — build 38 (1.0.2): собрать, прогнать Maestro, найти и починить всё

Адресат: терминал-Клод на Mac (доступ к EAS/симулятору/устройству/Maestro/SSH).
Прочитай ПОЛНОСТЬЮ перед стартом. Это не мелкая правка — это финальный прогон
перед Submit for Review. Работай методично, репортуй честно, ничего не скрывай.

---

## 0. Контекст: что произошло за сегодня (18.07.2026)

За один день из клауд-сессии внесён большой пакет изменений в ветку
`claude/youthful-cerf-barf3` (уже влита в `main`, но **build 37 на TestFlight
собран ДО этого** — в нём НИЧЕГО из списка ниже нет). Твоя задача — собрать
**build 38**, который включает всё это, и убедиться, что оно реально работает
на устройстве, а не только «компилируется».

### Чат (ядро сегодняшней работы)
- **Голосовые сообщения END-TO-END**: новый backend-эндпоинт
  `POST /api/v1/chat/voice` (`backend/api/chat.py`) — загрузка аудио в storage,
  возвращает ключ; фронт (`src/screens/ChatScreen.js` → `appendVoiceMessage`)
  грузит через `chatAPI.uploadChatVoice`, ключ идёт в общем поле `photo_url`
  с `is_voice=true`. `CHAT_VOICE_ENABLED = true`. **Раньше аудио НЕ грузилось
  вообще** — сейчас должно реально записываться, отправляться и играть у
  получателя.
- **Фото**: тап по 📷 теперь спрашивает Камера/Галерея (`Alert.alert`, native
  только — на web сразу галерея).
- **«Печатает…»**: `POST /api/v1/chat/typing` (in-memory TTL 6с) +
  `partner_typing` в ответе `GET /chat/messages/{room}`; фронт пингует при
  наборе текста (троттл 2.5с), показывает «✍️ печатает…» в шапке.
- **«Онлайн»**: `_LAST_SEEN` на бэке (обновляется в `send`/`get_messages`),
  `partner_online` в ответе; зелёная точка «в сети» рядом с именем в шапке
  чата, если активность моложе 90с.
- **✓/✓✓ прочитано** — уже было и работает (`is_read`), просто подтверждено.
- **Контекстные быстрые фразы**: `src/components/QuickPhrases.js` —
  ряд «по ситуации» меняется от `dealStatus` (accepted/in_progress/at_border),
  отдельно для роли водителя и клиента.
- **Фикс P1**: фото/голос раньше уходили БЕЗ `roomId` → попадали в отдельную
  «p:»-комнату и пропадали из открытой сделки через 3 сек (оптимистичный
  пузырь без `_optimistic`). Починено — теперь идут в ту же комнату сделки,
  с `clientMsgId` (идемпотентно).
- **Фикс**: слияние сообщений в `loadMessages` — текстовый фолбэк-дедуп
  срабатывал даже когда у серверного сообщения уже есть `clientMsgId`,
  из-за чего два одинаковых сообщения подряд («ок»/«ок») визуально
  схлопывались. Теперь фолбэк — только когда `clientMsgId` отсутствует.
- **Шапка чата теперь резолвит сделку из `roomId`**, если пришли не по
  `dealId` (вход из списка «Чаты») — раньше кнопка звонка/маршрут/карточка
  сделки не показывались при таком входе. Проверь оба пути входа в чат.

### Статус сделки и таймлайн
- Новый визуальный компонент `DealStatusTimeline`
  (`src/components/deal/DealRoom.js`): 🤝 Принят → 🚛 В пути →
  🛂 На границе → ✅ Доставлен. Подключён в `CargoDetail.js`.
- Новый статус **`at_border`** добавлен в backend `VALID` для
  `PATCH /market/deals/{id}/status`.
- **P0-класс фикс**: `POST /market/deals/{id}/location` (гео-трекинг) раньше
  ОТКЛОНЯЛ (409) координаты на статусе `at_border` — трекинг замирал ровно
  на границе. Починено — `at_border` добавлен в allow-list.
- Накладная (CMR): кнопка «📄 Накладная» на сделке →
  `POST /market/deals/{id}/waybill-link` (подписанная ссылка, TTL 7д) →
  `GET /market/deals/{id}/waybill` — печатный HTML-документ.

### Геолокация (фоновый GPS — НОВОЕ, требует РЕАЛЬНОГО устройства)
- `src/utils/backgroundLocation.js` — `expo-task-manager` + `expo-location`,
  таска `urtruck-deal-location`. Регистрируется на верхнем уровне через
  `import './src/utils/backgroundLocation'` в `App.js`.
- `app.json` обновлён: iOS `UIBackgroundModes += "location"` +
  `NSLocationAlwaysAndWhenInUseUsageDescription`; Android —
  `ACCESS_BACKGROUND_LOCATION` + `FOREGROUND_SERVICE(_LOCATION)`.
- Хук `useDealLocationBroadcast` (`src/hooks/useDealLocationBroadcast.js`)
  теперь стартует/стопит фоновый трекинг через
  `startBackgroundTracking()/stopBackgroundTracking()`, подключён на уровне
  `MainTabs` (`src/navigation/AppNavigator.js`), а не только в
  `MyTripsScreen` — работает с ЛЮБОГО экрана.
- **Это критично проверить на реальном устройстве**: симулятор фоновую
  геолокацию эмулирует плохо/не всегда честно. Нужен реальный iPhone,
  разрешение «Всегда» (Always), сворачивание приложения, движение или Location
  simulation через Xcode (Debug → Simulate Location) как минимум для базовой
  проверки, но финальная проверка — GPX route на реальном устройстве в
  движении, если возможно.
- `TrackTruckScreen` обогащён: осталось км, скорость км/ч, ETA, «на границе»,
  «обновлено N мин назад» (поллинг 10с, было 15с).

### Дизайн клиента — «Сочный мандарин»
- Глобальная замена акцента грузоотправителя `#F59E0B` → `#FF8400`
  (88 упоминаний по всему `src/`, включая `designV1.js`, `brandV2.js`,
  `designSystemV2.ts`, `theme.js`). **Водительский зелёный `#00E676` НЕ
  тронут** — обязательно проверь, что он остался как был на всех
  driver-экранах.
- Иерархия кнопок: главная CTA — плотная заливка; вторичные — без цветных
  рамок (мягкая подложка); опасные («Отменить сделку») — красный текст на
  бледном фоне без рамки. Затронуты: `MyTripsScreen.js`, `CargoDetail.js`,
  `FeedCard.js`, `TripDetail.js`.
- Карточки заказа: скругление 20, hairline-обводка, мягкая тень — без
  цветных ободков вокруг статуса.
- Анимации: `src/components/ui/FadeInUp.js` (`FadeInUp`/`PopIn`) — карточки
  списка появляются каскадом (выезд 10px + fade, шаг 50мс), статус
  «Принят» — пружинная галочка. `PressableScale` (сжатие 0.97) на CTA и
  кнопках сделки. `SegmentTabs` — `variant="underline"` для клиента
  (подчёркивание активной вкладки), водитель — прежний `pill`.
- **Проверь визуально на устройстве**: и светлую, и тёмную тему, обе роли,
  особенно что anимации не дёргаются/не роняют FPS на списке из многих
  карточек.

### Маркетплейс / жизненный цикл публикаций («Модель А»)
- Цена ОБЯЗАТЕЛЬНА везде (груз, рейс, включая редактирование груза — раньше
  через `EditCargoModal` можно было поставить 0 и вернуть «по договорённости»
  чёрным ходом — починено).
- Дата загрузки обязательна при создании груза (симметрично рейсу).
- Публикация живёт 3 дня (дата + 2), потом уходит из ленты. Напоминания
  владельцу +1/+2/+3 день. Продление ОДНИМ ТАПОМ «✅ Ещё актуально»
  (`POST /market/cargos/{id}/extend`, `/market/trips/{id}/extend`) — дата
  сбрасывается на сегодня, снова видно всем.
- Новый полноэкранный пикер города `src/components/LocationPickerModal.js`:
  поиск, недавние, избранное, популярные, погранпереходы, геолокация.
- Локализация названий городов на китайский/английский
  (`src/utils/places.js`) — **был баг с регистром** (`lang` приходит как
  `'ZH'`, а сравнение было с `'zh'`) — починен, но стоит перепроверить
  визуально на китайском интерфейсе, что города реально переводятся
  (Хоргос → 霍尔果斯 и т.п.), а не остаются кириллицей.
- inDrive-стиль: крупный селектор «Откуда → Куда» на ленте
  (`FeedScreen.js`), убрано «Направление» из мелких фильтров.

### Прочее backend
- `POST /admin/cleanup-stale-listings` (Basic Auth, `dry_run` флаг) —
  удаляет только просроченные и невидимые записи без ставок/сделок.
  **ВАЖНО: спроси владельца, запускал ли он это** (были 18 старых cargo +
  16 trips в проде, все с истёкшей датой — если ещё не почищено, лента
  публично пуста, `total: 0`). Если нет — можешь предложить это сделать
  (нужен `URTRUCK_ADMIN_USER`/`URTRUCK_ADMIN_PASS` из `backend/.env` на
  сервере).
- `no_bids_notify_job` — каждые 3 часа проверяет груз/рейс без ставок 18+ч,
  шлёт пуш-подсказку «скорректировать цену». Дедуп через `push_log.data_json`.
- Валюта в push-текстах ставок исправлена (было `$` хардкодом, теперь
  реальная `_money(amount, currency)`).
- Убран из публичного `GET /chat/translate/info` префикс OpenAI-ключа
  (был security-утечкой, хоть и малой).

### Документы (для контекста, не для правки)
- `docs/APP_OVERVIEW_FOR_DEVELOPERS.md` — полная логика приложения.
- `docs/CHAT_PUSH_SPEC.md` — детальная спека чата/пушей.
- `docs/DRIVER_DESIGN_SPEC.md` — все цвета/токены водителя.
- `qa/CHAT_QA_MASTER.md` — чек-лист чата (☁️-пункты уже зелёные,
  📱-пункты — твоя работа сейчас).
- `qa/PUSH_DEVICE_TEST.md` — Level 5 пуш-тест на реальном устройстве.
- `scripts/test_chat_api.sh` — безопасный живой тест API чата (12/12 green
  на момент написания; можешь перегнать для сверки).

---

## 1. Жёсткие ограничения (не нарушать)

- **НЕ отправляй build в App Store Review** — это делает владелец сам,
  вручную, после того как ты подтвердишь, что всё работает.
- **НЕ пушь реальным пользователям** — только тестовые актёры
  (`agent-boris` = client, `agent-serik` = driver — уже есть инфраструктура
  в `qa/maestro/_lib/qa-login.yaml` + `ensure-actor.js`/`.sh`).
- **НЕ трогай прод-данные** напрямую руками (кроме явно одобренного
  `cleanup-stale-listings` через API, и только если владелец подтвердил).
- **НЕ меняй бизнес-логику/дизайн без согласования** — если Maestro/ручной
  тест найдёт то, что выглядит как «неправильное поведение по замыслу»
  (а не баг), — спроси, не переделывай сам.
- **НЕ форсируй пуш, не амендь чужие коммиты, не пропускай хуки**
  (`--no-verify` и т.п.) — если хук блокирует коммит, разбирайся в причине.
- **НЕ меняй `app.json version`** вручную — версия остаётся `1.0.2`,
  `buildNumber` инкрементится автоматически EAS (`appVersionSource: remote`,
  `autoIncrement: true` в `eas.json`).
- Секреты (`backend/.env`, `certs/*`) не коммитить, не логировать целиком.

---

## 2. План работы

### Фаза 1 — Синхронизация
1. `git fetch origin main && git log --oneline -5 origin/main` — убедись,
   что видишь коммиты по сегодняшним фичам (голосовые, мандарин, фоновый GPS,
   waybill, no_bids). Если ветка `claude/youthful-cerf-barf3` на твоей
   машине расходится с `origin` — сверься, не затирая чужую работу
   (см. правило реконсиляции: `git fetch` + `git rebase origin/...`).
2. `npm install` (проверь, что `expo-task-manager` подтянулся — он новый
   в `package.json`).
3. `npx expo prebuild -p ios --clean` (native-проект должен подхватить
   новые `Info.plist`-ключи из `app.json` — `NSLocationAlwaysAndWhenInUse...`,
   `UIBackgroundModes`).

### Фаза 2 — Сборка build 38
4. `eas build --platform ios --profile production` (или твой обычный
   пайплайн). Дождись завершения, зафиксируй номер билда.
5. Установи на TestFlight / dev-client на реальный iPhone.

### Фаза 3 — Автоматический прогон (Maestro)
6. Прогони существующие флоу как smoke-тест базовой работоспособности:
   `qa/maestro/client-createcargo.yaml`, `client-cargodetail.yaml`,
   `client-deal-chat.yaml`, `client-offers-actions.yaml`,
   `chat_bid_notifications_e2e.yaml`, `driver-queue-cgr.yaml` — используй
   актёров `agent-boris`/`agent-serik` через `qa-login.yaml`.
7. **Напиши и прогони НОВЫЕ Maestro-флоу под сегодняшний функционал**
   (их ещё нет — не ищи готовые):
   - `chat-voice-message.yaml` — запись голосового, отправка, проверка что
     оно появилось у собеседника (можно через API-проверку `GET /chat/messages`
     на `is_voice=true` и наличие `photo_url`, если UI-ассерт на звук сложен
     в Maestro).
   - `chat-photo-camera-gallery.yaml` — оба пути (если Maestro может мокать
     камеру/пикер на симуляторе — иначе отметь как «только руками»).
   - `deal-status-timeline.yaml` — пройти весь путь accepted → in_progress →
     at_border → delivered с двух актёрских сессий, проверить, что таймлайн
     визуально подсвечивает правильный шаг на каждой стадии.
   - `deal-waybill.yaml` — тап «Накладная», проверить, что открывается
     валидная страница (может быть просто API-проверка статуса 200 на
     ссылку из ответа `waybill-link`).
   - `feed-mandarin-colors.yaml` (опционально) — если Maestro/скриншот-diff
     умеет проверять цвет пикселя кнопки — сверить, что акцент клиента
     реально `#FF8400`, а не старый `#F59E0B` (иначе — ручная визуальная
     проверка со скриншотом).
8. Все находки Maestro (краши, неверный текст, зависшие экраны) — фиксируй
   с шагами воспроизведения, скриншотом/логом.

### Фаза 4 — Ручная проверка на реальном устройстве (то, что облако не может)
Пройди **📱-пункты** из `qa/CHAT_QA_MASTER.md` и `qa/PUSH_DEVICE_TEST.md`
буквально по чек-листу, отмечая ✅/❌. Ключевые:
- Голосовое: записать → отправить → **на ВТОРОМ устройстве получить и
  прослушать**. Это самое непроверенное сегодня — облако физически не может
  протестировать запись/воспроизведение звука.
- Камера/галерея — оба пути отправки фото.
- Фоновый GPS: водитель принимает сделку → сворачивает приложение (не
  закрывает полностью) → едет/имитирует движение → **на втором устройстве
  клиент видит обновление позиции на «Где машина»** без открытого приложения
  водителя.
- Push на локскрин (< 5 сек), deep-link по тапу, suppress в открытой
  комнате, бейдж на иконке.
- Кнопка 📞 звонка — реальный номер набирается.
- «Печатает…» и «в сети» — видно у собеседника в реальном времени.
- Накладная — открывается, печатается/сохраняется в PDF корректно.
- Визуально: мандариновый акцент клиента, зелёный водителя не задет,
  анимации плавные, обе темы (светлая/тёмная).

### Фаза 5 — Фиксы
9. Всё найденное чини **в тот же день**, приоритет P0 → P1 → P2. Если фикс
   трогает файлы вне зоны сегодняшних изменений (регистрация, скоринг и
   т.п.) или это архитектурное решение — сначала спроси владельца.
10. После фиксов — пересобери (build 39, если нужно) и повтори проблемные
    пункты чек-листа, не весь список заново.

### Фаза 6 — Отчёт
11. Верни владельцу структурированный отчёт: что прогнано, что зелёное,
    что красное (с шагами и скринами), что починено на месте, что осталось
    и почему. **Не сабмить в Apple** — это финальная кнопка владельца.

---

## 3. Критерий готовности («когда можно звать владельца жать Submit»)

- [ ] Build 38 собран, установлен на реальное устройство.
- [ ] Голосовые: записано/отправлено/получено/прослушано на 2 устройствах.
- [ ] Фоновый GPS: клиент видит движение водителя при свёрнутом приложении
      водителя.
- [ ] Push доходят на локскрин, deep-link ведёт куда надо, suppress работает.
- [ ] Таймлайн сделки проходит все 4 статуса без сбоев на обоих устройствах.
- [ ] Накладная открывается и печатается.
- [ ] Мандариновый акцент клиента виден, водительский зелёный не задет.
- [ ] Лента не пустая (после чистки БД + при наличии тестовых/реальных
      публикаций) — водитель видит грузы, клиент видит рейсы.
- [ ] Все P0/P1 находки закрыты; P2 — задокументированы, не блокируют.

Когда всё зелёное — сообщи владельцу явно: «Готово к Submit», и жди его
команды. Ты НЕ отправляешь сам.
