# CHAT E2E DEEP TEST REPORT — UrTruck deal chat (Variant B)

## 1. Ветка / коммит
`integration/build-30 @ 318789a` (+ этот отчёт). Без EAS/TestFlight/buildNumber/прод/merge.

## 2. Окружение
- Тип проверки: **локальный API-E2E** через FastAPI `TestClient` (реальные роутеры
  `marketplace` + `chat` + `notifications`), `require_level` застаблен для смены актёра.
- БД: изолированная SQLite (`/tmp/e2e.db`), схемы применены как в продакшен-старте
  (marketplace/deals/notifications/chat + миграция канонических комнат).
- Симулятор/устройство: **в этой среде недоступны** → Maestro/скриншоты/APNS не
  прогонялись здесь (см. §11), помечены REAL DEVICE REQUIRED.

## 3. Актёры
- `ownerX` — грузовладелец «Борис Владелец»
- `driverY` — водитель «Серик Водитель»
- `thirdZ` — посторонний (негативные тесты)

## 4. Данные прогона
- cargo1 (Бахты→Москва, Электросамокаты, $7500), cargo2 (Иу→Астана, $8888)
- bid_id выдан; **room_id (cargo1) = `e26df430-…` (один у обеих ролей)**

## 5. Инвариант канонической комнаты
| Проверка | Результат |
|---|---|
| тот же cargo+owner+bidder → один room_id | ✅ |
| разные cargo (та же пара) → разные room_id | ✅ |
| дубль-ставка не плодит комнату (409 «уже есть активная ставка») | ✅ |
| третий не читает/не шлёт (403/403) | ✅ |
| легаси-комнаты не падают (миграция) | ✅ (test_deal_rooms) |

## 6. Синхронизация сообщений
- driver → owner: `driver-msg-test-001` — владелец видит ✅
- owner → driver: `owner-reply-test-001` — водитель видит ✅
- одинаковая история у обоих, без дублей, не исчезает при повторном открытии ✅

## 7. Точки входа в чат
Все ведут в одну каноническую комнату (room_id из ставки == room_id в `/chat/rooms`
у обеих ролей == комната, куда пишет каждая сторона). Партнёр определяется по
`partner_id` из бэка: owner видит «Серик Водитель», driver — «Борис Владелец»,
**«Собеседник» не появляется** ✅. (Открытие из ленты/уведомления/таба «Чаты» на
устройстве — §11.)

## 8. Payload уведомления (код `api/chat.py` send_to_user.data)
`{type:"chat_message", room_id, cargo_id, bid_id, sender_id, recipient_id}` ✅.
Ставка возвращает `room_id`; bid-уведомление: url `/cargos/{id}?bid={bid}`.

## 9. Колокольчик (bell)
Владелец получает запись `bid_created` «Новое предложение $…» в `/notifications` ✅.

## 10. Бейдж «Чаты» (in-app)
`/chat/unread`: до чтения = 1, после открытия чата = 0 ✅. Считается по
непрочитанным `chat_messages` получателя; своё сообщение не учитывается;
залипания нет.

## 11. Готовность push
- Payload сформирован и корректен (§8) ✅ — проверено локально.
- **APNS на локскрин: REAL DEVICE REQUIRED** (не тестировалось здесь).
- **Фоновая доставка: REAL DEVICE REQUIRED**.
- Maestro-флоу/скриншоты обеих ролей: **не прогонялись в этой среде** (нет
  симулятора). Рекомендуется добить на машине с симулятором (терминальная сессия):
  driver-submit-bid-open-chat / bidirectional-chat-sync / notifications-open-correct-chat.

## 12. Негативные тесты
| Кейс | Ожидание | Факт |
|---|---|---|
| третий читает комнату | 403 | ✅ 403 |
| третий шлёт в комнату | 403 | ✅ 403 |
| send без room_id и без to_user_id | 400 | ✅ 400 |
| дубль активной ставки | блок | ✅ 409 |
| получатель из участников (мусорный to_user_id игнор) | владельцу | ✅ (test_deal_rooms) |
| self-fallback убран | да | ✅ (код ChatScreen) |

## 13. Регрессии / гейты
- `backend/tests/test_deal_rooms.py` — 5/5 ✅
- bid/chat-зависимые тесты в `test_bid_actions.py` — проходят; падающие
  `my_dashboard_*`/`list_bids` — **pre-existing** (идентично на оригинале) ✅
- `npm run qa:i18n` — 1575×4, 0 пропусков ✅
- `npm run qa:ux` — ✅ (обновлён устаревший change-role чек)
- babel parse изменённых JS — ✅
- backend import (chat+marketplace) — ✅

## 14. Логи/доказательства
- E2E-прогон (вывод в сессии): A/A12/B/E/D + инвариант + G — все ✅.
- `backend/tests/test_deal_rooms.py` (в репозитории).

## 15. Найденные баги
- **P0 (исправлено):** сообщения не доходили между ролями (краш `/send` при
  to_user_id=null + комнаты по паре). Закрыто Вариантом B.
- P1/P2/P3 по чату в этом прогоне — **не найдено.**
- Замечание (не баг): тест-окружению нужны PRO-колонки drivers_registration
  (`legal_form` и т.п.) — в проде есть; локально добавлены для прогона.

## 16. Решение по гейту
**READY FOR BUILD** — весь локальный/API-доказуемый функционал зелёный.
Оставшийся обязательный шаг — **device-QA**: APNS-пуш (локскрин/фон) и Maestro-
скриншоты обеих ролей на симуляторе/TestFlight.

## 17. Прямые ответы
- Водитель и владелец делят один room_id? **ДА**
- Сообщения синхронизируются в обе стороны? **ДА**
- Уведомления открывают правильную комнату? **ДА** (payload+url с room_id;
  in-app deep-link парсится в App.js; тап на устройстве — §11)
- Бейдж чата работает? **ДА** (in-app unread 1→0)
- APNS доказан на реальном устройстве? **НЕТ** (REAL DEVICE REQUIRED)
- Оправдана ли ещё сборка сейчас? **ДА** — API-доказательства зелёные; сборка
  нужна именно чтобы добить device-QA (APNS + Maestro) на реальном билде.
