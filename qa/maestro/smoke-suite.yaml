appId: com.urtruck.app
name: UrTruck Maestro smoke suite
tags:
  - smoke
  - release
---
# Аутентифицированный водитель проверяется отдельным driver-auth.yaml с
# QA-токеном. В базовом smoke не используем устаревшие сценарии из _obsolete.
- runFlow: client-tabs.yaml
# registration/verification запускается отдельно: после гостевого входа
# предыдущего флоу Expo Go сохраняет экран Main, поэтому общий suite не может
# честно вернуться на Onboarding без сброса всего контейнера Expo Go.
