# Environment
.env
.env.local
.env.production
.env.*.local

# Dependencies
node_modules/
.expo/
__pycache__/
*.pyc
venv/
.venv/

# Build
dist/
build/
web-build/
*.egg-info/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Auto-generated by Expo (проект на JS, не нужен в git)
tsconfig.json

# Logs
*.log
npm-debug.log*

# Database
*.sqlite
*.sqlite3
*.db

# Secrets
*.pem
*.key
*.cert
*.p12
backend/certs/
deploy.sh

# Tests
TEST_RESULTS.md
TOLIK_REPORT.md
coverage/

# Playwright
test-results/
playwright-report/
blob-report/
tests/e2e/*.png

# Design references (large PNG mockups — kept locally, not in git)
design/UrTruck_New_Design_v1/*.png
qa/debug/dumps/

# Android signing secrets — НИКОГДА в git (guard для будущих файлов).
# Существующий keystore/urtruck-release.keystore уже в истории — вынести
# из репо и сбросить upload key отдельно (см. RELEASE_SIGNING.md).
*.keystore
*.jks
*.p12
!android/app/debug.keystore
upload_certificate.pem
keystore/
credentials.json
*.aab
google-play-service-account.json

# Claude Code harness runtime state (PID/timestamp, session-local — не версионируется)
.claude/scheduled_tasks.lock
